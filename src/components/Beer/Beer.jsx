import React from 'react'

const Beer = (props) => {

    // const { name, image_url, tagline, description } = props;
    
  return (
    <>
    <p>{props.name}</p>
    </>
  );
};

export default Beer;